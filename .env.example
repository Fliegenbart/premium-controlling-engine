# Optional: Claude API Key für KI-Kommentierung
# Kann auch zur Laufzeit in der UI eingegeben werden
ANTHROPIC_API_KEY=sk-ant-...

# Docker Compose: Netzwerk-Bindings
# Empfehlung: Ollama nicht öffentlich exponieren (Default: localhost-only).
APP_BIND_ADDRESS=0.0.0.0
OLLAMA_BIND_ADDRESS=127.0.0.1

# Security / Hardening
# Demo-User nur in Dev aktiv; in Produktion standardmäßig AUS.
ENABLE_DEMO_USERS=false

# Optional: initialen Admin bei leerer User-Datenbank erzeugen (nur wenn ENABLE_DEMO_USERS=false)
ADMIN_BOOTSTRAP_EMAIL=
ADMIN_BOOTSTRAP_PASSWORD=
ADMIN_BOOTSTRAP_NAME=Administrator

# Direkte SQL-API standardmäßig deaktiviert
QUERY_API_ENABLED=false
QUERY_API_TOKEN=

# Dokumenten-API absichern (serverseitig, NICHT NEXT_PUBLIC)
DOCUMENT_ACCESS_TOKEN=
ALLOW_QUERY_TOKEN=false
