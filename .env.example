# Premium Controlling Engine - Environment Configuration
# Copy to .env and adjust values

# ============================================
# LOCAL LLM (DEFAULT - NO API NEEDED)
# ============================================

# Ollama settings (default: Docker service)
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=qwen2.5:14b

# Alternative models:
# OLLAMA_MODEL=qwen2.5:7b      # Faster, less RAM
# OLLAMA_MODEL=qwen2.5:32b     # More powerful, needs 32GB+ RAM
# OLLAMA_MODEL=mistral:7b      # Alternative
# OLLAMA_MODEL=llama3.1:8b     # Meta's model

# ============================================
# OPTIONAL: CLOUD API (Anthropic)
# ============================================

# Uncomment to enable cloud mode (falls back to local if empty)
# ANTHROPIC_API_KEY=sk-ant-api...

# ============================================
# DATABASE
# ============================================

# DuckDB persistence path
DATABASE_PATH=/data/controlling.duckdb

# ============================================
# PRODUCTION (optional)
# ============================================

# Domain for SSL (if using Traefik)
DOMAIN=localhost
ADMIN_EMAIL=admin@example.com

NODE_ENV=production
PORT=3000
